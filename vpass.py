# -*- coding:utf-8 -*-

from bs4 import BeautifulSoup
import requests
from basescr import BaseScr

class Vpass(BaseScr):
	def scrape(self)
	if(self.page==0):
		print('Page was not loaded, exiting...')
		return False
	payload={'':self.user, '':self.password,}
